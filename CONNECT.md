# Как зайти на сервер (без Tailscale у игроков)

Игроки **ничего не качают** — только Minecraft и адрес.

## Сейчас у тебя

| | |
|--|--|
| Сервер | `./start.sh` запущен, порт **25565** |
| Wi‑Fi (дома) | `192.168.0.85` |
| Интернет (для всех) | **`90.151.80.16`** — после проброса порта на роутере |

В логе **нет** чужих IP → к тебе реально никто не подключался. Исправляем ниже.

---

## Вариант A — друг рядом / та же Wi‑Fi

Друг в Minecraft → **Прямое подключение** → `192.168.0.85`  
Версия **Java 1.21.4**.

Ты смотришь консоль `./start.sh` — должно быть `ЕгоНик[/192.168.0.XX:...] logged in`.  
Нет строки → не та версия / не Java / не тот адрес.

---

## Вариант B — любой с интернета (как мини-VPS дома)

### 1. Проброс порта на роутере

1. Браузер → http://192.168.0.1 (логин с наклейки роутера).
2. Port Forwarding / NAT / «Виртуальные серверы»:
   - **Внешний порт:** 25565  
   - **Внутренний IP:** 192.168.0.85 (твой Mac)  
   - **Внутренний порт:** 25565  
   - **TCP**
3. Сохранить.

### 2. Mac — разрешить Java

Системные настройки → **Сеть** → **Файрвол** → Параметры → разрешить **Java** / **openjdk** (входящие).

### 3. Адрес для игроков

```text
90.151.80.16
```

Minecraft → **Прямое подключение** → `90.151.80.16`  
Версия **1.21.4**.

Запиши в `public-host.txt` (одна строка).

### 4. Проверка

Пусть друг зайдёт с **мобильного интернета** (не твой Wi‑Fi) — так проверяешь именно интернет, не локалку.

В консоли сервера должна появиться строка с его ником.

---

## Если с `90.151.80.16` всё равно никто не заходит

Часто провайдер даёт **серый IP (CGNAT)** — проброс с дома **не работает**.

Тогда только:
- **VPS** (нормальный вариант для юзеров), или
- туннель **только на твоём Mac** (playit и т.п.) — **игрокам** ничего ставить не надо, они вводят выданный адрес.

---

## VPS (когда заведёшь)

Тот же `world/`, `plugins/`, `./start.sh`.  
Игрокам: **IP VPS** вместо `90.151.80.16`. Всё.

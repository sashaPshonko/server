# Бесплатно: проброс порта на роутере (как на VPS)

Друзья подключаются к **внешнему IP** провайдера — как к VPS, без VPN-приложений.

## 1. Сервер на Mac

```bash
./start.sh
```

## 2. Роутер

1. Открой http://192.168.0.1 (логин на наклейке).
2. **Port Forwarding** / «Виртуальные серверы» / NAT:
   - Имя: `minecraft`
   - Внешний порт: **25565**
   - Внутренний IP: **192.168.0.85** (IP Mac с сервером)
   - Внутренний порт: **25565**
   - Протокол: **TCP**
3. Сохрани.

## 3. Внешний IP

На Mac:
```bash
curl -4 -s ifconfig.me
```

Впиши в `public-host.txt` — друзья: **Прямое подключение** → этот IP.

## 4. Ограничения

- IP может **меняться** при перезагрузке роутера (у некольких провайдеров).
- Некоторые провайдеры **блокируют** входящие на 25565 — тогда только Tailscale.
- Mac должен быть **включён**, сервер запущен.

## Mac: разрешить Java

Системные настройки → Сеть → Файрвол → **Java** / **openjdk** — разрешить входящие.
